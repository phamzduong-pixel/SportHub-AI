import { Check, Download, Eye, Printer, X } from 'lucide-react';
import { useEffect, useMemo, useState } from 'react';
import { Link, useNavigate, useParams } from 'react-router-dom';
import { Badge, Button, EmptyState, Input, LoadingSkeleton, PageHeader, useToast } from '@/components/common';
import { RefundStatusPanel } from '@/components/refunds/RefundStatusPanel';
import { apiRequest } from '@/services/apiClient';
import { cancelManagedBooking, completeManagedBooking, confirmManagedBooking, getManagedRefunds, getPaymentSummary, noShowManagedBooking, rejectManagedBooking, startManagedBooking, type ApiBooking, type PaymentSummary, type RefundRequest } from '@/services/customerApi';

const money = (value: number) => `${value.toLocaleString('vi-VN')}đ`;
const statusLabel: Record<string, string> = { pending_payment: 'Chờ đặt cọc', pending_confirmation: 'Chờ xác nhận', confirmed: 'Đã xác nhận', in_progress: 'Đang diễn ra', completed: 'Hoàn thành', no_show: 'Không đến sân', cancelled: 'Đã hủy', cancelled_by_customer: 'Khách đã hủy', cancelled_by_owner: 'Chủ sân đã hủy', expired: 'Hết hạn', rejected: 'Đã từ chối' };
const paymentLabel: Record<string, string> = { unpaid: 'Chưa thanh toán', pending: 'Đang xử lý', partial: 'Đã đặt cọc', paid: 'Đã thanh toán', refund_pending: 'Chờ hoàn tiền', refund_overdue: 'Hoàn tiền quá hạn', refunded: 'Đã hoàn tiền', disputed: 'Đang khiếu nại', failed: 'Không thành công', cancelled: 'Đã hủy' };
const listBookings = () => apiRequest<{ items: ApiBooking[]; total: number }>('/bookings?page_size=100');

export function ManagementBookingsPage() {
  const { toast } = useToast(); const [items, setItems] = useState<ApiBooking[]>([]); const [loading, setLoading] = useState(true); const [query, setQuery] = useState(''); const [group, setGroup] = useState('today'); const [busy, setBusy] = useState<number>();
  const load = () => listBookings().then((result) => setItems(result.items));
  useEffect(() => { load().catch((e) => toast(e instanceof Error ? e.message : 'Không tải được booking.', 'error')).finally(() => setLoading(false)); }, []);
  const rows = useMemo(() => items.filter((item) => { const today = new Date().toISOString().slice(0, 10); const grouped = group === 'today' ? item.booking_date === today : group === 'upcoming' ? item.booking_date > today && !['cancelled', 'cancelled_by_customer', 'cancelled_by_owner', 'expired', 'completed', 'no_show'].includes(item.status) : group === 'cancelled' ? ['cancelled', 'cancelled_by_customer', 'cancelled_by_owner', 'expired', 'rejected'].includes(item.status) : item.status === group; return grouped && `${item.booking_code} ${item.customer_name} ${item.customer_phone || ''} ${item.field_name} ${item.booking_date}`.toLowerCase().includes(query.toLowerCase()); }), [items, query, group]);
  const decide = async (item: ApiBooking, accept: boolean) => { const reason = accept ? undefined : window.prompt('Nhập lý do từ chối booking (bắt buộc):')?.trim(); if (!accept && (!reason || reason.length < 3)) return toast('Phải nhập lý do từ chối ít nhất 3 ký tự.', 'error'); setBusy(item.id); try { const updated = accept ? await confirmManagedBooking(item.id) : await rejectManagedBooking(item.id, reason); setItems(items.map((x) => x.id === updated.id ? updated : x)); toast(accept ? 'Đã xác nhận booking.' : 'Đã từ chối; tiền cọc chuyển sang chờ hoàn.', 'success'); } catch (e) { toast(e instanceof Error ? e.message : 'Không thể cập nhật booking.', 'error'); } finally { setBusy(undefined); } };
  const transition = async (item: ApiBooking, action: 'start' | 'complete' | 'no_show') => { setBusy(item.id); try { const updated = action === 'start' ? await startManagedBooking(item.id) : action === 'complete' ? await completeManagedBooking(item.id) : await noShowManagedBooking(item.id); setItems((current) => current.map((entry) => entry.id === updated.id ? updated : entry)); toast('Đã cập nhật trạng thái booking.', 'success'); } catch (error) { toast(error instanceof Error ? error.message : 'Chuyển trạng thái không hợp lệ.', 'error'); } finally { setBusy(undefined); } };
  const exportCsv = () => { const content = ['Mã,Khách hàng,Sân,Tổng tiền,Đã cọc,Còn lại,Thanh toán,Booking', ...rows.map((x) => [x.booking_code, x.customer_name, x.field_name, x.total_amount, Math.min(x.paid_amount, x.deposit_amount), x.remaining_amount, paymentLabel[x.payment_status], statusLabel[x.status]].join(','))].join('\n'); const a = document.createElement('a'); a.href = URL.createObjectURL(new Blob(['\ufeff', content])); a.download = 'bookings.csv'; a.click(); URL.revokeObjectURL(a.href); };
  const groups = [['today','Hôm nay'],['upcoming','Sắp tới'],['pending_confirmation','Chờ xác nhận'],['in_progress','Đang diễn ra'],['completed','Đã hoàn thành'],['cancelled','Đã hủy'],['no_show','No-show']];
  return <><PageHeader title="Quản lý booking" description="Dữ liệu thật của các sân thuộc OWNER, được lọc theo đúng quyền OWNER." actions={<Button variant="outline" leftIcon={<Download size={16} />} onClick={exportCsv}>Xuất CSV</Button>} /><div className="mb-4 flex gap-1 overflow-x-auto">{groups.map(([value,label]) => <button key={value} className={`shrink-0 rounded-lg px-3 py-2 text-sm font-semibold ${group === value ? 'bg-brand-50 text-brand-700' : 'text-slate-500'}`} onClick={() => setGroup(value)}>{label}</button>)}</div><input className="field mb-4" placeholder="Tìm mã, tên khách, số điện thoại, sân hoặc ngày" value={query} onChange={(e) => setQuery(e.target.value)} />{loading ? <LoadingSkeleton lines={8} /> : rows.length ? <div className="overflow-x-auto rounded-card border bg-white"><table className="w-full min-w-[1200px] text-left text-sm"><thead className="bg-slate-50"><tr>{['Mã booking','Khách hàng','Sân / lịch','Tổng tiền','Đã cọc','Còn lại','Thanh toán','Trạng thái','Hành động'].map((x) => <th className="px-4 py-3" key={x}>{x}</th>)}</tr></thead><tbody>{rows.map((item) => <tr className="border-t" key={item.id}><td className="px-4 py-3 font-bold text-brand-700">{item.booking_code}</td><td className="px-4 py-3">{item.customer_name}<small className="block text-slate-500">{item.customer_phone || item.customer_email}</small></td><td className="px-4 py-3">{item.field_name}<small className="block">{item.booking_date} · {item.start_time_snapshot.slice(0,5)}–{item.end_time_snapshot.slice(0,5)}</small></td><td className="px-4 py-3 font-semibold">{money(item.total_amount)}</td><td className="px-4 py-3">{money(Math.min(item.paid_amount, item.deposit_amount))}</td><td className="px-4 py-3">{money(item.remaining_amount)}</td><td className="px-4 py-3"><Badge>{paymentLabel[item.payment_status]}</Badge></td><td className="px-4 py-3"><Badge>{statusLabel[item.status] || item.status}</Badge></td><td className="px-4 py-3"><div className="flex gap-2">{item.status === 'pending_confirmation' && <><Button size="sm" loading={busy === item.id} leftIcon={<Check size={15} />} onClick={() => void decide(item, true)}>Xác nhận</Button><Button size="sm" variant="danger" disabled={busy === item.id} leftIcon={<X size={15} />} onClick={() => void decide(item, false)}>Từ chối</Button></>}{item.status === 'confirmed' && <><Button size="sm" loading={busy === item.id} onClick={() => void transition(item, 'start')}>Bắt đầu</Button><Button size="sm" variant="danger" disabled={busy === item.id} onClick={() => void transition(item, 'no_show')}>No-show</Button></>}{item.status === 'in_progress' && <Button size="sm" loading={busy === item.id} onClick={() => void transition(item, 'complete')}>Hoàn tất</Button>}<Link to={`/management/bookings/${item.id}`}><Button size="sm" variant="outline" leftIcon={<Eye size={15} />}>Chi tiết</Button></Link></div></td></tr>)}</tbody></table></div> : <EmptyState title="Không có booking" description="Không tìm thấy dữ liệu phù hợp trong nhóm này." />}</>;
}

export function ManagementBookingDetailPage() {
  const { bookingId = '' } = useParams(); const navigate = useNavigate(); const { toast } = useToast(); const [booking, setBooking] = useState<ApiBooking>(); const [summary, setSummary] = useState<PaymentSummary>(); const [refund, setRefund] = useState<RefundRequest>(); const [reason, setReason] = useState(''); const [loading, setLoading] = useState(true); const [busy, setBusy] = useState(false);
  const load = () => Promise.all([apiRequest<ApiBooking>(`/bookings/${bookingId}`), getPaymentSummary(Number(bookingId)), getManagedRefunds()]).then(([b, s, r]) => { setBooking(b); setSummary(s); setRefund(r.items.find((item) => item.booking_id === b.id)); });
  useEffect(() => { load().catch((e) => toast(e instanceof Error ? e.message : 'Không tải được booking.', 'error')).finally(() => setLoading(false)); }, [bookingId]);
  const decide = async (accept: boolean) => { if (!booking) return; if (!accept && reason.trim().length < 3) return toast('Phải nhập lý do từ chối.', 'error'); setBusy(true); try { setBooking(accept ? await confirmManagedBooking(booking.id) : await rejectManagedBooking(booking.id, reason.trim())); toast(accept ? 'Đã xác nhận booking.' : 'Đã từ chối và tạo yêu cầu hoàn tiền.', 'success'); if (!accept) await load(); } catch (error) { toast(error instanceof Error ? error.message : 'Không thể xử lý booking.', 'error'); } finally { setBusy(false); } };
  const ownerCancel = async () => { if (!booking || reason.trim().length < 3) return toast('Phải nhập lý do hủy.', 'error'); setBusy(true); try { setBooking(await cancelManagedBooking(booking.id, reason.trim())); toast('Đã hủy booking và tạo yêu cầu hoàn tiền.', 'success'); await load(); } catch (error) { toast(error instanceof Error ? error.message : 'Không thể hủy booking.', 'error'); } finally { setBusy(false); } };
  if (loading) return <LoadingSkeleton lines={8} />; if (!booking) return <EmptyState title="Không tìm thấy booking" description="Booking không tồn tại." />;
  return <><button onClick={() => navigate(-1)} className="mb-4 text-sm text-brand-700">← Quay lại</button><PageHeader title={booking.booking_code} description={`${booking.customer_name} · ${booking.customer_email}`} actions={<><Badge>{statusLabel[booking.status]}</Badge><Badge>{paymentLabel[booking.payment_status]}</Badge></>} />{['pending_confirmation', 'confirmed'].includes(booking.status) && <div className="mb-5 rounded-card border border-amber-200 bg-amber-50 p-4"><div><b>{booking.status === 'pending_confirmation' ? `Khách đã thanh toán đủ tiền cọc ${money(booking.deposit_amount)}` : 'Chủ sân có thể chủ động hủy khi không thể phục vụ'}</b><p className="text-sm text-amber-800">Nếu từ chối/hủy, hệ thống hoàn toàn bộ số tiền khách đã trả và khóa thanh toán còn lại.</p></div><Input className="mt-3" label="Lý do từ chối / hủy *" value={reason} onChange={(event) => setReason(event.target.value)} placeholder="Nhập ít nhất 3 ký tự" /><div className="mt-3 flex gap-2">{booking.status === 'pending_confirmation' && <Button loading={busy} onClick={() => void decide(true)}>Xác nhận</Button>}<Button variant="danger" disabled={busy} onClick={() => void (booking.status === 'pending_confirmation' ? decide(false) : ownerCancel())}>{booking.status === 'pending_confirmation' ? 'Từ chối' : 'Chủ động hủy'}</Button></div></div>}<div className="grid gap-5 lg:grid-cols-2"><section className="rounded-card border bg-white p-5"><h2 className="font-bold">Thông tin lịch chơi</h2><p className="mt-4"><b>Cơ sở:</b> {booking.location}</p><p><b>Sân:</b> {booking.field_name} · {booking.sport_type}</p><p><b>Ngày:</b> {booking.booking_date}</p><p><b>Khung giờ:</b> {booking.start_time_snapshot.slice(0,5)}–{booking.end_time_snapshot.slice(0,5)} ({booking.duration_minutes} phút)</p></section><section className="rounded-card border bg-white p-5"><h2 className="font-bold">Tóm tắt thanh toán</h2><div className="mt-4 space-y-2"><Row label="TỔNG CỘNG" value={booking.total_amount} /><Row label="Đã cọc" value={Math.min(booking.paid_amount, booking.deposit_amount)} /><Row label="Còn lại" value={booking.remaining_amount} />{booking.refund_status === 'refund_pending' && <Row label="Chờ hoàn cọc" value={booking.refundable_deposit_amount || booking.deposit_amount} />}</div><Button className="mt-5 print:hidden" variant="outline" leftIcon={<Printer size={16} />} onClick={() => window.print()}>In phiếu</Button></section></div>{refund && <RefundStatusPanel initial={refund} mode="owner" onChanged={setRefund} />}<section className="mt-5 rounded-card border bg-white p-5"><h2 className="font-bold">Giao dịch</h2>{summary?.transactions.map((tx) => <div key={tx.id} className="mt-3 flex justify-between border-t pt-3 text-sm"><span>{tx.transaction_code} · {tx.payment_type === 'deposit' ? 'Đặt cọc' : tx.payment_type === 'refund' ? 'Hoàn tiền' : 'Thanh toán còn lại'}</span><b>{money(tx.amount)} · {paymentLabel[tx.refund_status] || paymentLabel[tx.status] || 'Đang xử lý'}</b></div>)}</section></>;
}
function Row({ label, value }: { label: string; value: number }) { return <div className="flex justify-between border-b pb-2"><span>{label}</span><b>{money(value)}</b></div>; }

