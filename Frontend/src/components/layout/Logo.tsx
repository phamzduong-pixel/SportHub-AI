import { Activity } from 'lucide-react'; import { Link } from 'react-router-dom';
export function Logo({ compact = false, inverted = false }: { compact?: boolean; inverted?: boolean }) { return <Link to="/" className={`flex items-center gap-2 font-bold tracking-tight ${inverted ? 'text-white' : 'text-slate-900'}`}><span className="grid h-9 w-9 place-items-center rounded-lg bg-brand-600 text-white"><Activity size={21} strokeWidth={2.5} /></span>{!compact && <span className="text-xl">SportHub <b className={inverted ? 'text-emerald-300' : 'text-brand-600'}>AI</b></span>}</Link>; }
