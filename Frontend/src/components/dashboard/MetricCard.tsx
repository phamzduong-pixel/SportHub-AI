import { ArrowDownRight, ArrowUpRight } from 'lucide-react'; import type { DashboardMetric } from '@/types/index';
export function MetricCard({ metric }: { metric: DashboardMetric }) { const up = metric.trend === 'up'; return <article className="rounded-card border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm font-medium text-slate-500">{metric.label}</p><div className="mt-3 flex items-end justify-between gap-2"><strong className="text-2xl font-bold tracking-tight text-slate-900">{metric.value}</strong><span className={`flex items-center text-xs font-semibold ${up ? 'text-emerald-600' : 'text-red-600'}`}>{up ? <ArrowUpRight size={15} /> : <ArrowDownRight size={15} />}{metric.change}</span></div><p className="mt-2 text-xs text-slate-400">so với tháng trước</p></article>; }
