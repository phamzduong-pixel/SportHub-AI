import type { Booking } from '@/types/index'; import { Badge } from '@/components/common';
const variants = { 'Đã xác nhận': 'info', 'Chờ xác nhận': 'warning', 'Đã hoàn thành': 'success', 'Đã hủy': 'neutral' } as const;
export function BookingTable({ data }: { data: Booking[] }) { return <div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr>{['Mã đặt', 'Khách hàng', 'Sân & khung giờ', 'Tổng tiền', 'Trạng thái'].map((item) => <th key={item} className="px-5 py-3 font-semibold">{item}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{data.map((booking) => <tr key={booking.id} className="hover:bg-slate-50/60"><td className="px-5 py-4 font-bold text-brand-700">{booking.id}</td><td className="px-5 py-4 font-medium text-slate-800">{booking.customer}</td><td className="px-5 py-4"><b className="block text-slate-700">{booking.venue}</b><span className="text-xs text-slate-500">{booking.date} • {booking.time}</span></td><td className="px-5 py-4 font-semibold">{booking.amount.toLocaleString('vi-VN')}đ</td><td className="px-5 py-4"><Badge variant={variants[booking.status]} dot>{booking.status}</Badge></td></tr>)}</tbody></table></div>; }
